# K2 — Termination Letter Mapping

ทำ **หนังสือบอกเลิกสัญญา** จากข้อมูล K2 ต้องดึงอะไรจากไหน · ทำจริงเมื่อ 2026-08-26

**ที่มา:** `ตัวอย่างข้อมูลที่ใช้ในหนังสือบอกเลิกสัญญา.xlsx` (33 คอลัมน์ · 3 ตัวอย่างที่ทีมกรอกไว้)
ถอดสูตรจากตัวอย่าง แล้ว**ตรวจกับฐานจริงทุกช่อง**

---

## Join path

ทุกอย่างเริ่มจากเลขสัญญา ไม่มี FK ในฐาน — เส้นทางเหล่านี้ทดสอบแล้วทั้งหมด

```
CONTRACT (CONTRACT_NUMBER)
   ├─ PERSON_ID   ──► PERSON ──► MT_PREFIX              ชื่อ + คำนำหน้า
   │                     └────► ADDRESS                 ที่อยู่ชุด A1 (ทะเบียนบ้าน)
   │                              ├─► MT_PROVINCE
   │                              ├─► MT_DISTRICT
   │                              └─► MT_SUB_DISTRICT
   ├─ PRODUDCT_ID ──► PRODUCT ──► MT_BRAND              เครื่อง + เงื่อนไขการเงิน
   │                     └──────► MT_CATEGORY
   ├─ CONTRACT_ID ──► CUSTOMER_CARD                     การ์ดลูกหนี้ 1 แถว = 1 งวด
   └─ CONTRACT_ID ──► COLLECTION_OD                     snapshot หนี้ค้างรายวัน
```

---

> **SQL ที่ดึงครบทุกช่องนี้แล้ว:** `sql/k2_termination_list_v2.sql`
> ทำเป็น Excel: `scripts/k2/k2_termination_v2_to_excel.py` (คอลัมน์ตัวหนังสือเขียนเป็นสูตร `=BAHTTEXT` ให้อัตโนมัติ)
> ตรรกะการคัด → [[K2 - OD6 Selection Logic]]

## Mapping table — 33 columns

| # | คอลัมน์ในหนังสือ | ที่มา | หลักการคิด |
|---|---|---|---|
| A | สัญญาเลขที่ | `CONTRACT.CONTRACT_NUMBER` | คีย์ตั้งต้น |
| B | ชื่อผู้ทำสัญญา | `MT_PREFIX.Prefix_name` + `PERSON.FIRST_NAME` + `PERSON.LAST_NAME` | `PERSON.PREFIX` เป็นรหัส ต้อง join `MT_PREFIX.Prefix_ID` แปลงเป็นคำ |
| C | วันที่ทำสัญญา | `CONTRACT.CONTRACT_START` | `MAKE_DATE` ให้ค่าเดียวกัน |
| D | ประเภทสัญญา | `'เช่าซื้อ ' + MT_CATEGORY.CATEGORY_NAME` | ผ่าน `PRODUCT.PRODUCT_CATEGORY` |
| E | ยี่ห้อ | `MT_BRAND.BRAND_NAME` | ผ่าน `PRODUCT.PRODUCT_BAND` (typo — ที่จริงคือ BRAND) |
| F | รุ่น/แบบ | `PRODUCT.MODEL_NAME` | |
| G | เครื่อง | `PRODUCT.MODEL_NUMBER` | |
| H | Serial Number | **`PRODUCT.SERIAL_NUMBER`** | ⚠️ ห้ามใช้ `CONTRACT.SERIAL_NUMBER` — ว่างทั้งตาราง |
| I | ค่าเช่าซื้อรวม VAT | **คำนวณ** `K × M` | ตรงกับ `PRODUCT.HP_VAT_SUM` 3 จาก 4 ราย (อีกรายต่าง 0.05 จากการปัดเศษ) |
| J | ↳ txt | **สูตร Excel** `=BAHTTEXT(I…)` | |
| K | งวดละ | **`PRODUCT.INSTALL_SUM`** | ⚠️ รวม VAT แล้ว · `INSTALL_AMT` เป็นยอดก่อน VAT อย่าใช้ |
| L | ↳ txt | `=BAHTTEXT(K…)` | |
| M | จำนวนงวด | **`PRODUCT.INSTALL_NUM`** | ⚠️ อย่าใช้ `CONTRACT.INSTALL_NUM_FINAL` — หมายถึงงวดที่ค่างวดปกติ น้อยกว่าจริง 1 งวด |
| N | วันเริ่มชำระงวดแรก | `PRODUCT.FRIST_PAY_DATE` | (typo) · เท่ากับ `MIN(CUSTOMER_CARD.DUEDATE)` |
| O | วันที่ชำระงวดต่อไป | `PRODUCT.DUEDATE_NUM` | มีแค่ 1 กับ 16 ทั้งฐาน |
| P | ค้างชำระงวดแรก | **คำนวณ** | `MIN(CUSTOMER_CARD.INSTALL_NUM)` ที่ `RECEIPT_NUMBER IS NULL` |
| Q | งวดที่ครบ OD 6 | ~~`MIN(P + 5, M)`~~ | ⚠️ **สูตรนี้ผิดกับคนเลี้ยง OD** — ใช้ `MIN(EXTRACT_DATE)` ที่ค้างครบ 6 งวดแทน → [[K2 - OD6 Selection Logic]] |
| R | ค่าเช่าซื้อ 6 งวด | **`COLLECTION_OD.OD_AMOUNT`** | ⚠️ ไม่ใช่ `(Q−P+1) × K` — ระบบนับเฉพาะงวดที่**เลยกำหนดแล้ว** |
| S | ↳ txt | `=BAHTTEXT(R…)` | |
| T | ค่าเบี้ยปรับชำระล่าช้า | `COLLECTION_OD.PENALTY_AMT` | ขั้น OD × 100 บาท → [[K2 - Fee Policy]] |
| U | ↳ txt | `=BAHTTEXT(T…)` | |
| V | ค่าติดตามทวงถามหนี้ | `COLLECTION_OD.COLLECT_AMT` | ไต่ขั้นตามตาราง แยก 2 กรณีตามว่าค่างวดถึง 1,000 ไหม → [[K2 - Fee Policy]] |
| W | ↳ txt | `=BAHTTEXT(V…)` | |
| X | ค่าใช้จ่ายอื่นตามกฎหมาย | **ค่าคงที่ 0** | ทั้ง 4 รายเป็น 0 · ไม่พบคอลัมน์ที่ตรงกันในฐาน |
| Y | ↳ txt | `=BAHTTEXT(X…)` | |
| Z | รวมเป็นเงินทั้งสิ้น | **คำนวณ** `R + T + V + X` | ดูข้อสังเกตท้ายหน้า |
| AA | ↳ txt | `=BAHTTEXT(Z…)` | |
| AB | วันที่เป็น OD6 | `COLLECTION_OD.EXTRACT_DATE` **+ 543** | วัน snapshot ที่ใช้เป็นเกณฑ์ · เก็บเป็น พ.ศ. ในไฟล์ |
| AC | ที่อยู่ตามทะเบียนบ้าน | **ประกอบ** `AD + AE + 'จังหวัด' + AF + AG` | |
| AD | เลขที่ | `ADDRESS` ชุด `A1_*` | ต่อกันพร้อมป้ายกำกับ — ดูสูตรล่าง |
| AE | แขวง/ตำบล/อำเภอ | `MT_SUB_DISTRICT` + `MT_DISTRICT` | `'ตำบล'+SUB_DISTRICT_NAME+' อำเภอ'+DISTRICT_NAME` |
| AF | จังหวัด | `MT_PROVINCE.PROVINCE_NAME` | ⚠️ `A1_PROVINCE` เก็บรหัสเป็นข้อความ ต้อง `TRY_CAST(... AS int)` |
| AG | รหัส ปณ. | `ADDRESS.A1_POSTALCODE` | |

**7 ช่องเป็นสูตร `=BAHTTEXT()` ไม่ใช่ข้อความ** (J, L, S, U, W, Y, AA) — ถ้าเขียนไฟล์ด้วย openpyxl ต้องเขียนเป็นสูตรกลับไป และต้องเปิดด้วย Excel เท่านั้นถึงจะประเมินค่า (LibreOffice / Google Sheets ไม่มีฟังก์ชันนี้)

### Address assembly formula

```
เลขที่ {A1_NO} หมู่ที่ {A1_MOI} หมู่บ้าน/โครงการ {A1_VILLAGE} อาคาร {A1_BUILDING}
ชั้น {A1_FLOOR} ซอย {A1_SOI} ถนน {A1_ROAD} เลขที่ห้อง {A1_ROOM_NO}
```

**ช่องไหนว่างให้ตัดทั้งท่อนรวมป้ายกำกับ** ไม่ใช่ปล่อยป้ายลอย
ค่าที่มีอยู่จริงอาจเป็น `-` หรือ `ไม่มี` ก็ได้ (ทีมกรอกไว้แบบนั้น) — ตัวอย่างในไฟล์แสดงทั้ง 3 แบบ

---

## Selection criteria

```sql
COLLECTION_OD.EXTRACT_DATE    = วันหมุดของรอบ
COLLECTION_OD.CONTRACT_STATUS = 48                     -- Overdue 6
AND EXISTS (SELECT 1 FROM CUSTOMER_CARD k
            WHERE k.CONTRACT_ID = ct.CONTRACT_ID
              AND k.DUEDATE = วันครบกำหนดของรอบนั้น
              AND k.RECEIPT_NUMBER IS NULL)             -- ยังไม่มีใบเสร็จ
AND COLLECTION_OD.OD_AMOUNT > 0                         -- ตัดคนที่จ่ายครบแล้วแต่สถานะค้าง
```

---

## ⚠️ OD6 status refreshes on the 3rd and 18th, not the 2nd and 17th

ข้อค้นพบที่สำคัญที่สุดของงานนี้ · จำนวนสัญญาสถานะ Overdue 6 รายวัน:

| วันที่ (2026) | OD6 | |
|---|---:|---|
| 25 ก.ค. – 2 ส.ค. | **6** | นิ่งอยู่ที่ 6 ทุกวัน |
| **3 ส.ค.** | **337** | ← รอบของสัญญาที่ครบกำหนดวันที่ **1** |
| 4–14 ส.ค. | 337 → 309 | ลดลงทุกวันเพราะคนทยอยจ่าย |
| 15–17 ส.ค. | **300** | |
| **18 ส.ค.** | **603** | ← รอบของสัญญาที่ครบกำหนดวันที่ **16** |
| 19–26 ส.ค. | 594 → 572 | |

เกิดซ้ำแบบเดียวกันทุกเดือน: **18 พ.ค. → 300 · 18 มิ.ย. → 293 · 18 ก.ค. → 302**

**แปลว่า** วันที่ 2 และ 17 คือ**วันที่ลูกค้าเป็น OD6 จริง** (วันถัดจากงวดที่ 6 ที่ไม่จ่าย) แต่ระบบเพิ่งบันทึกสถานะในวันถัดมา

ผลต่อการคัดรายชื่อรอบ ส.ค. 2569:

| เกณฑ์ | ได้ |
|---|---:|
| extract 2 ส.ค. + งวด 1 ส.ค. ยังไม่จ่าย | **0** |
| extract 17 ส.ค. + งวด 16 ส.ค. ยังไม่จ่าย | **1** |
| extract **3** ส.ค. + งวด 1 ส.ค. ยังไม่จ่าย | **236** |
| extract **18** ส.ค. + งวด 16 ส.ค. ยังไม่จ่าย | **227** |

**ถ้าจะทำหนังสือทั้งรอบต้องใช้วันที่ 3 และ 18** (รวม 463 สัญญา) แล้วค่อยใส่ 2 กับ 17 ในช่อง "วันที่เป็น OD6" `[อนุมาน]`

> ⚠️ **เกณฑ์นี้ถูกแทนที่แล้ว** — `CONTRACT_STATUS = 48` เป็นแค่ป้าย ไม่ใช่จำนวนงวดค้างจริง และ snapshot วันเดียวพลาดคนเลี้ยง OD
> เกณฑ์ที่ใช้จริงตอนนี้ให้ **208 สัญญา** → [[K2 - OD6 Selection Logic]]

---

## The Overdue 6 population

snapshot 2026-08-26 · **572 สัญญา**

| แบ่งตามวันครบกำหนด | สัญญา |
|---|---:|
| วันที่ 16 | 300 |
| วันที่ 1 | 272 |

| จำนวนงวดที่ค้าง (`NUMBER_OF_OD_INSTALLMENT`) | สัญญา |
|---|---:|
| 6 งวด | 190 |
| 5 งวด | 240 |
| 4 งวด | 42 |
| 3 งวด | 41 |
| 2 งวด | 29 |
| 1 งวด | 24 |
| **0 งวด** | **6** |

**สถานะ OD6 เหนียว** — ขึ้นแล้วไม่ลดลงแม้จ่ายบางส่วน จึงมี 6 สัญญาที่สถานะเป็น OD6 แต่ยอดค้างเป็น 0 (จ่ายครบแล้ว) **ต้องตัดออกก่อนออกหนังสือ** ไม่งั้นจะออกหนังสือบอกเลิกสัญญาให้คนที่ไม่ได้ค้าง

ความครบถ้วนของข้อมูล 572 ราย: ชื่อ / ที่อยู่ / serial / รุ่น / ค่างวด / วันครบกำหนด **ครบ 100%** · ขาดแค่ยอด OD 6 ราย

---

## Observations from the sample file

**ยอดรวมของ 3 ตัวอย่างบวกไม่ลงตัวกับรายการย่อยของตัวเอง**

| แถว | R+T+V+X | ยอดรวมที่กรอกไว้ | ต่าง |
|---|---:|---:|---:|
| พัสกร | 2,016.20 | 2,066.20 | +50 |
| ทิพวัลย์ | 2,720.93 | 2,770.93 | +50 |
| นราธิป | 1,737.72 | 3,525.44 | +1,787.72 |

**อธิบายได้แล้ว 1 จาก 3 แถว หลังได้เอกสารนโยบายค่าธรรมเนียม** ([[K2 - Fee Policy]])

| แถว | ตีความใหม่ | ผล |
|---|---|---|
| **นราธิป** | ตอนกรอกอยู่ที่ **OD2** → 2 × 1,587.72 + ค่าปรับ 200 + ค่าติดตาม 150 | = **3,525.44** ✅ ตรงเป๊ะ |
| พัสกร · ทิพวัลย์ | OD1 · เข้าสูตรได้ถ้าค่าติดตามงวดแรกเป็น **100** ไม่ใช่ 50 | ยังอธิบายไม่ครบ |

→ **ค่า `R` ในไฟล์ตัวอย่างแถวนราธิปกรอกผิด** (ใส่ 1 งวด ทั้งที่ยอดรวมคิดจาก 2 งวด) · ยอดรวมต่างหากที่ถูก

**ตัวอย่างมีข้อมูลผิดเทียบกับฐาน 2 จุด**

| จุด | ในไฟล์ | ในฐาน |
|---|---|---|
| ยี่ห้อของแถว "ทิพวัลย์" | Apple | **Xiaomi** (รุ่นก็เขียนว่า Xiaomi 14T pro) |
| ค้างชำระงวดแรกของแถว "นราธิป" | 18 | **17** (ค้างทั้งงวด 17 และ 18) |

**ประเภทสัญญาเขียน "เช่าซื้อ Smart Phone" ทุกแถว** ทั้งที่แถวแรกเป็น MacBook (Laptop) และแถวที่สามเป็น iPad (Tablet) — ถ้าจะให้ถูกควรดึงจาก `MT_CATEGORY` ตามจริง

---

## Traps to watch for

**`CUSTOMER_CARD` ไม่เก็บประวัติ** — `RECEIPT_NUMBER` อ่านค่า ณ วันที่รันคิวรี่ ไม่ใช่สถานะของวัน snapshot
ถ้ารันย้อนหลัง คนที่จ่ายไปแล้วหลังวันหมุดจะหลุดออกจากรายการ **ควรรันและบันทึกผลในวันเดียวกับที่ออกหนังสือ** `[อนุมาน]`

**ยอดค้างตามการ์ด ≠ ยอด OD ของระบบ** — ตัวอย่างจริง: สัญญาหนึ่งค้าง 13 งวดตามการ์ด แต่ 8 งวดยังไม่ถึงกำหนด ระบบจึงนับ OD แค่ 5 งวด **หนังสือต้องใช้ยอดที่เรียกเก็บได้จริง** คือของระบบ

**เงื่อนไขการเงินเก็บซ้ำ 2 ที่** — `QUOTATION` กับ `PRODUCT` · ใช้ `PRODUCT` เพราะผูกกับสัญญาโดยตรงผ่าน `PRODUDCT_ID` (join ติด 288,197 จาก 288,207)

**ปีในไฟล์เป็น พ.ศ.** — ช่อง "วันที่เป็น OD6" เก็บเป็น datetime ปี 2569 ส่วนช่องวันที่อื่นเป็น ค.ศ. **ผสมกันในไฟล์เดียว** ต้องระวังตอนคำนวณ

---

## Scripts used

อยู่ที่ `scripts\k2\` — อ่านฐานอย่างเดียว credential จาก env `K2_USER` / `K2_PWD`

| ไฟล์ | ทำอะไร |
|---|---|
| `scripts\k2\k2_survey.py` | ดึง metadata ทั้งฐานลง `_raw/` |
| `scripts\k2\k2_samples.py` | ดึง sample rows แบบ mask PII |

สคริปต์เติม Excel เขียนแบบใช้ครั้งเดียวใน scratchpad — ถ้าจะทำเป็นงานประจำควรย้ายมาไว้ข้างกันแล้วรับ**วันหมุด**เป็นพารามิเตอร์

---

## Template ใหม่ (สิงหาคม 2026)

ทีมส่ง template รุ่นใหม่มา — `Template จดหมายบอกเลิก.xlsx` · รับเข้า vault 2026-08-28
โครงสร้างเดิมยังใช้ได้ แต่**มี 3 อย่างเปลี่ยน**

### 1. รองรับทั้ง K2 และ ITOS ในไฟล์เดียว

ตัวอย่างในไฟล์มีทั้ง 2 แบบปนกัน:

| เลขสัญญา | ระบบ | วันครบกำหนด |
|---|---|---|
| `TFF2510-005889` · `TFF2601-017868` · `TFF2508-002031` · `TFF2602-011149` | **ITOS** | 14 · 26 · 27 · 16 |
| `2495733` · `25221018` · `24143298` · `25192436` | **K2** | 16 · 1 · 1 · 16 |

**เลขขึ้นต้น `TFF` = ITOS · ตัวเลขล้วน = K2**

⚠️ **สูตรเดิมที่สมมติว่าวันครบกำหนดเป็นวันที่ 1 หรือ 16 ใช้กับสัญญา ITOS ไม่ได้**
ต้องอ่าน `วันที่ชำระงวดต่อไป` จากข้อมูลจริง — ฝั่ง ITOS พบวันที่ 14, 20, 21, 22, 23, 26, 27
รายละเอียด → [[ITOS Overview]]

### 2. เพิ่มคอลัมน์ "รวมจำนวนงวดที่ค้าง"

| ค่า | ความหมาย | ต้องทำอะไร |
|---|---|---|
| **= 6** | เคสมาตรฐาน — ค้างครบ 6 งวดพอดี | ออกหนังสือตามปกติ |
| **≠ 6** | สัญญาสิ้นสุดก่อนครบ 6 งวดค้าง | **ต้องหมายเหตุว่าเป็นเคสหมดอายุสัญญา** |

ในตัวอย่างที่ให้มา พบค่า `1` และ `3` ปนกับ `6` — ยืนยันว่าเคสหมดอายุสัญญาเกิดขึ้นจริงและต้องแยกจัดการ
ตรรกะการคัด → [[K2 - OD6 Selection Logic]]

### 3. แตกที่อยู่เป็น 4 คอลัมน์

จากเดิมที่รวมเป็นข้อความเดียว ตอนนี้แยกเป็น:

`ที่อยู่ตามทะเบียนบ้าน` (ข้อความเต็ม) · `เลขที่` · `แขวง/ตำบล/อำเภอ` · `จังหวัด` · `รหัส ปณ.`

ดึงจาก `ADDRESS` บล็อก "ตามทะเบียนบ้าน" ได้ตรงๆ ไม่ต้องประกอบข้อความก่อน → [[K2 - Customer & Address]]

> **ข้อสังเกตคุณภาพข้อมูล:** ตัวอย่างแถวแรกมีค่า `295 หมู่ที่หมู่ที่ 13` — คำว่า "หมู่ที่" ซ้ำสองครั้ง
> เกิดจากสูตรประกอบที่เติมคำนำหน้าให้ค่าที่มีคำนำหน้าอยู่แล้ว → [[Data Standardization & Quality]]

### คอลัมน์ทั้งหมดใน template ใหม่ (sheet `ข้อมูล`)

สัญญาเลขที่ · ชื่อผู้ทำสัญญา · วันที่ทำสัญญา · ประเภทสัญญา · ยี่ห้อ · รุ่น/แบบ · เครื่อง · Serial Number ·
ค่าเช่าซื้อรวมภาษีมูลค่าเพิ่ม (ตัวเลข + ตัวหนังสือ) · งวดละ (ตัวเลข + ตัวหนังสือ) · จำนวนงวดเช่าซื้อ ·
วันเริ่มชำระงวดแรก · วันที่ชำระงวดต่อไป · ค้างชำระงวดแรก · งวดที่ครบ OD 6 ·
ค่าเช่าซื้อ 6 งวด (OD1–OD6) (ตัวเลข + ตัวหนังสือ) · ค่าเบี้ยปรับชำระล่าช้า (ตัวเลข + ตัวหนังสือ) ·
ค่าติดตามทวงถามหนี้ (ตัวเลข + ตัวหนังสือ) · ค่าใช้จ่ายอื่นที่บริษัทฯ มีสิทธิเรียกเก็บ (ตัวเลข + ตัวหนังสือ) ·
รวมเป็นเงินทั้งสิ้น (ตัวเลข + ตัวหนังสือ) · วันที่เป็น OD6 · **รวมจำนวนงวดที่ค้าง** ·
ที่อยู่ตามทะเบียนบ้าน · เลขที่ · แขวง/ตำบล/อำเภอ · จังหวัด · รหัส ปณ.

ทุกยอดเงินต้องมี**คู่ตัวหนังสือ** — ใช้ `=BAHTTEXT()` ใน Excel

---

## เชื่อมกับโน้ตอื่น

[[K2 Overview]] · [[K2 - OD6 Selection Logic]] · [[K2 - Business Rules]] · [[K2 - Fee Policy]] · [[K2 - Query Cookbook]] · [[K2 - Collection & OD]] · [[K2 - Contract & Account]] · [[K2 - Payment & Invoice]] · [[K2 - Customer & Address]]
