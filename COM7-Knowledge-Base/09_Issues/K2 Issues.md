# K2 Issues

คำถามและงานค้างของระบบ K2 (`HPCOM7`)

แยกออกมาจากโน้ตเนื้อหา เพื่อให้โน้ตความรู้อ่านได้ต่อเนื่องไม่มีอะไรมาคั่น

---

## K2 — Business Rules — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Business Rules]]

- `PERSON.PAY_DAY` เก็บอะไร ลูกค้าเลือกได้ระหว่างวันที่ 1 กับ 16 ใช่ไหม
- ทำไม `CONTRACT.SERIAL_NUMBER` ถึงว่างทั้งตาราง เคยใช้แล้วเลิก หรือไม่เคยใช้เลย
- ผู้ค้ำ 11,508 คนที่ `CONTRACT_ID` ว่าง — ใบคำขอไม่ผ่าน หรือแค่ไม่ได้เติมกลับ
- `OA_Score` คำนวณจากอะไร ใครเป็นคนให้
- `RELATION_REF` (int) กับ `RELATION_REF_DES` (ข้อความ) — มี master ของความสัมพันธ์ไหม (`MT_RELATIONSHIP_REF` มี 5 แถว)
- 33 แถวที่ `CONTRACT_ID` กับ `CONTRACT_NUMBER` ใน `REPAYMENT` ไม่ตรงกัน — เกิดจากอะไร

---

## K2 — Collection & OD — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Collection & OD]]

- ประวัติหนี้ค้างก่อน 2026-04-08 อยู่ที่ไหน — `Temp_History_OD` ใช่ไหม
- `COLLECTION_OD` snapshot รายวันตลอดไป หรือมี retention
- `COLLECTION_OD_ASSIGNMENT` (98,477) มอบหมายเฉพาะบางกลุ่มหรือทั้งหมด
- `REMARK_1` / `REMARK_2` ใน `CONTACT_DEBT_COLLECTION` มี PII ปนแค่ไหน
- สัญญาที่ "ขายหนี้" (4,015) ขายให้ใคร ข้อมูลยังอยู่ในระบบไหม
- เครื่องที่ยึดคืน (Repo) บันทึกที่ไหน `ASSETS_INFORMATION_PFUND` ใช่หรือเปล่า

---

## K2 — Contract & Account — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Contract & Account]]

- `CreditScrolling_Logs_Contract` เขียนทุกกี่วินาที ต้อง ingest ไหม
- `FLAG_SEND_S9` / `DATE_SEND_S9` บน `CONTRACT` ส่งไประบบอะไร ("S9")
- `JURISTIC` — ลูกค้านิติบุคคลมีกี่ราย เก็บอะไรบ้าง
- `CHECKER` (402,965) กับ `CHECKER_GUARANTOR` (361,169) — ผู้ค้ำจริงมีกี่คน ใช้ข้อมูลได้ไหมในแง่ legal
- ข้อมูล NCB ที่ดึงมาเก็บไว้นานแค่ไหน ต้องลบตามกฎหมายเมื่อไหร่
- `PRODUCT` กับ `QUOTATION` เก็บเงื่อนไขการเงินซ้ำกัน — อันไหนคือค่าที่ผูกพันตามสัญญา

---

## K2 — Customer & Address — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Customer & Address]]

- `CONFIRM_PDPA` / `DS_PDPA` มีค่าจริงกี่แถว consent เก็บครบตั้งแต่เมื่อไหร่
- ไฟล์แนบ (`*_FILE`) เก็บ base64 ในฐานจริงไหม ขนาดเท่าไหร่ ต้องกันออกจาก ingestion หรือเปล่า
- `PERSON.PASSWORD` เก็บอะไร hash หรือไม่
- `Disease_ID` / `Narcotic_ID` มีค่าจริงกี่แถว — ถ้ามีมากต้องยกระดับการจัดการเป็นข้อมูลอ่อนไหวชั้นสูง
- ทำไม `CIF_PERSON_ID` ถึงมีในทุกตารางหลักแต่ไม่ถูกใช้ — เคยมีแผนทำ CIF แล้วหยุดไป?
- `PROSPECT_CUSTOMER` เอาไปใช้ทำการตลาดได้ไหมในแง่ consent

---

## K2 — Data Dictionary (Core Tables) — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Data Dictionary]]

- `FLAG_SEND_S9` / `DATE_SEND_S9` — "S9" คือระบบอะไร
- `ABM_STARTDATE` / `ABM_NUMBER` ใน `PRODUCT` — ABM ย่อมาจากอะไร
- `PERSON.PASSWORD` เก็บ hash หรือ plaintext
- `PERSON.PAY_DAY` — ลูกค้าเลือกวันที่ 1 หรือ 16 ได้เองใช่ไหม
- ตาราง ★★ 40 ตัวยังไม่มี field-level doc — ตัวไหนควรทำก่อน
- `TTA_PROVINCE` / `TTA_DISTRICT` ต่างจาก `MT_*` ยังไง

---

## K2 — Fee Policy (penalty · collection fee · payment allocation) — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Fee Policy]]

- นโยบาย "OD6 ไม่เก็บค่าทวงถามเพิ่ม" เริ่มใช้เมื่อไหร่ ระบบทำตามแล้วหรือยัง
- ค่าติดตามงวดแรกเคยเป็น 100 แล้วลดเหลือ 50 เมื่อไหร่ — `FeeControl` มีแถวเดียวไม่มีประวัติ
- `FeeControl.discount = 50` ใช้ตอนไหน เกี่ยวกับ `PENALTY_WAVE_AMT` / `COLLECT_WAVE_AMT` ใน `REPAYMENT` ไหม
- กฎ 1,000 บาท กับตารางขั้น OD เก็บไว้ที่ไหนในระบบ (ไม่พบในฐาน)
- เอกสารนี้ประกาศใช้วันไหน มีเวอร์ชันก่อนหน้าไหม
- "เกิน 7 วัน" นับจากวันครบกำหนด หรือจากตอน 23:00 ที่คิดค่าปรับ

---

## K2 — Master & Setup — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Master & Setup]]

- `TTA_PROVINCE` / `TTA_DISTRICT` / `TTA_SUB_DISTRICT` ต่างจาก `MT_*` ยังไง ใช้ตัวไหนเป็นหลัก
- PFund คือผลิตภัณฑ์อะไร ยัง active ไหม
- `Ascend_Name_En` ใน `MT_OCCUPATION` map ไประบบอะไร
- `MT_FACULTY` 39,475 แถว import มาจากไหน อัปเดตยังไง
- `SETUP_PARTNER` id 1027, 1028 ชื่อว่าง — พาร์ทเนอร์ที่เลิกแล้วหรือข้อมูลไม่ครบ

---

## K2 — OD6 Selection Logic — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - OD6 Selection Logic]] · คำถามเตรียมคุยทีม → [[Collection Team Questions]]

- คนที่เคยค้างครบ 6 งวดแล้วจ่ายลดลงมา **ยังมีสิทธิ์ถูกบอกเลิกสัญญาไหม** — 18 รายในผลลัพธ์อยู่ในสถานะนี้ · **ต้องให้ legal ตอบ**
- **ยอดในหนังสือใช้ของการ์ดหรือของระบบ** — ต่างกัน 490,534 บาททั้งชุด · **ต้องให้ฝ่ายบัญชียืนยัน**
- คนที่ค้าง 1–2 งวดตอนสัญญาหมดอายุ ควรได้หนังสือทันทีไหม หรือให้ติดตามปกติก่อน
- มีผู้ค้ำแค่ **1 รายจาก 578** — ถ้าแผนคือส่งหนังสือถึงผู้ค้ำด้วย แทบไม่มีใครให้ส่ง
- 52 รายไม่มีข้อมูลใน `COLLECTION_OD` snapshot ล่าสุด (ยอดคงเหลือ/วันชำระล่าสุดว่าง)
- `COLLECTION_OD` มีข้อมูลย้อนหลังแค่ตั้งแต่ **2026-04-08** (~140 วัน) คนที่แตะ 6 งวดก่อนหน้านั้นแล้วเลี้ยงมาตลอด ตรวจย้อนไม่ได้
- รอบการออกหนังสือจริงคือเดือนละครั้ง หรือทุกวันที่ 3 และ 18
- ควรใช้ยอด ณ วันที่แตะ 6 งวด หรือยอด ณ วันออกหนังสือ — ตอนนี้ใช้อย่างหลัง

---

## K2 — Payment & Invoice — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Payment & Invoice]]

- `TTP_` ย่อมาจากอะไร ใครเป็นเจ้าของชั้นนี้
- `TRANSACTION_REPAY` (9.7M) เลิกใช้แล้วจริงไหม (ไม่อัปเดตตั้งแต่ 2022-03)
- `PDF_FORM` เก็บ binary ในฐานหรือเก็บแค่ path
- `REPAY_TYPE` 3 (ค่าเบี้ยปรับ) กับ 5 (ค่าเช่าซื้อ) ใช้จริงไหม
- แถวที่ `REPAY_DATE` เป็นปี 1067 มีกี่แถว มีข้อมูลผิดปีแบบอื่นอีกไหม
- ช่องทางรับชำระอื่นนอก SCB บันทึกที่ไหน

---

## K2 — Termination Letter Mapping — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 - Termination Letter Mapping]]

- "ค่าใช้จ่ายอื่นที่บริษัทฯ มีสิทธิเรียกเก็บตามกฎหมาย" มาจากไหน ทำไมเป็น 0 ทุกราย
- ยอดรวมที่เกินมา 50 บาทในสองตัวอย่าง คืออะไร — เข้าเค้าว่าค่าติดตามงวดแรกเคยเป็น 100 ดู [[K2 - Fee Policy]]
- สถานะ OD6 ที่ไม่ลดลงเมื่อจ่ายบางส่วน — ตั้งใจหรือเป็นบั๊ก
- หนังสือที่ออกไปแล้วบันทึกกลับเข้าระบบที่ไหน (`PDF_NOTICE` 17,903 แถว ใช่หรือเปล่า)
- ใครเป็นคนกดออกหนังสือ และรอบการออกคือทุกวันที่เท่าไหร่

---

## K2 Overview — คำถามที่ยังไม่มีคำตอบ

เนื้อหา → [[K2 Overview]]

- `CreditScrolling_Logs_Contract` **239.7 ล้านแถว** เป็นตารางใหญ่สุดของฐาน มี 15 คอลัมน์ (`CONTRACT_ID`, `STATUS_ID`, `CreditScrolling_Logs_Contract_DateTime`) เข้าเค้าว่าเป็น log การเปลี่ยนสถานะสัญญา `[อนุมาน]` — แต่ 239M แถวต่อ 288k สัญญา = เฉลี่ย 832 แถวต่อสัญญา **มากผิดปกติ ต้องถาม MIS-Fintech ว่าเขียนทุกกี่วินาที และต้อง ingest ไหม**
- **`EGG_*` 65 views** จาก 165 views ทั้งหมด (`EGG_PERSON`, `EGG_ADDRESS`, `EGG_MDM`, `EGG_BANK_STATEMENT`) — "EGG" คือระบบอะไร นี่คือ integration layer ที่มีอยู่แล้วหรือเปล่า อ่าน definition ไม่ได้เพราะไม่มีสิทธิ์
- `UFUNDInterface` (2,029,152 แถว) มีคอลัมน์ `Payload`, `URL`, `Status`, `PostingDate` = คิว API ส่งออก — ส่งไปที่ไหน
- `NCAP_*` (`NCAP_PAYMENT` 2M, `NCAP_STATUS` 1M, `NCAP_INFORMATION` 132k) — NCAP คืออะไร `NCAP_INFORMATION` มี `FULL_NAME`, `TAX_ID`, `ADDRESS_REGIST`, `INCOME` ครบ เข้าเค้าว่าเป็น interface กับสถาบันการเงินภายนอก `[อนุมาน]`
- `HPAP_*` (100–200 แถว) กับ `*_PFUND` / `*_UPHONE` — ผลิตภัณฑ์ที่ยังไม่ launch หรือเลิกแล้ว
- `CIF_PERSON_ID` มีอยู่ในหลายตารางแต่ **ว่าง 404,645 จาก 404,749 แถว** — ตั้งใจจะทำ CIF แล้วไม่ได้ทำ หรือกำลังจะทำ ถ้าจะทำจริงนี่คือจุดที่ควรเสียบ SSOT key
- ยังไม่ได้เทียบว่าสัญญาใน K2 ซ้ำกับ ITOS ไหม — คำถามเดิมที่ยังเปิดอยู่
- `COLLECTION_OD` เก็บ snapshot ตั้งแต่ 2026-04-08 (ตารางถูกสร้างวันนั้น) — ก่อนหน้านั้นเก็บที่ไหน `Temp_History_OD` (5,067,003) ใช่หรือเปล่า

## การคัดสัญญาบอกเลิก — ต้องตัดสินใจ (2026-08-28)

| # | เรื่อง | ใครตอบ |
|--:|---|---|
| 1 | **เงื่อนไข B ของ query ยกเลิกสัญญา** — "สัญญาหมดอายุแล้วยังค้าง" 156 สัญญา แต่ **155 ราย ค้างจริงแค่ 1–5 งวด** ไม่ถึง 6<br>คอมเมนต์ในโค้ดเขียนว่าต้อง "> 6 เดือน" แต่ SQL ไม่ได้เช็ค → จะยึดแบบไหน | **legal + ทีมติดตามหนี้** |
| 2 | กลุ่ม A 48 รายที่สถานะ 48 แต่ค้างจริง < 6 งวด (คนเลี้ยง OD) ออกหนังสือได้ไหม | **legal** |
| 3 | `COLLECTION_OD_ASSIGNMENT` มี snapshot แค่ 2026-08-04 และ 2026-08-19 แต่ไฟล์ที่ส่งจริงระบุ extract 2026-08-17 — ไฟล์นั้นมาจากไหน | **ทีมที่ทำไฟล์ส่ง** |
| 4 | จำนวนสัญญาสถานะ 48 ใน `COLLECTION_OD` (300–603) ต่างจากไฟล์ที่ส่งทีมติดตาม (608) มาก — สองตัวเลขนี้ควรตรงกันไหม | **MIS-Fintech** |

**ทางเลือกที่คำนวณไว้แล้ว**

| ตีความ | เงื่อนไข | สัญญา |
|---|---|---:|
| ปล่อยตามเดิม | — | 621 |
| ค้างครบ 6 งวดจริง | `AND N_PASTDUE >= 6` | 418 |
| ค้างเกิน 6 เดือน | `AND DATEDIFF(month, PERIOD_DUE_DATE, @snap) >= 6` | 5 |

เนื้อหา → [[K2 - OD6 Selection Logic]] · คำถามเตรียมคุยทีม → [[Collection Team Questions]]

---

---

## เชื่อมกับโน้ตอื่น

[[Issue Index]] · [[Open Questions & Risks]] · [[Current Status]] · [[Home]]
